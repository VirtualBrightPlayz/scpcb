#include "Console.h"
#include "GUI/GUIComponent.h"

Console::Console(World* wrld, UIMesh* um, Font* font, KeyBinds* kb, Config* con, TxtManager* tm, PGE::IO* io) : Menu(wrld, "console") {
    float frameWidth = GUIComponent::SCALE_MAGNITUDE;
}